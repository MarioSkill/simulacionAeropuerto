#include <random>
#include <iostream>
#include <time.h>

class Utils {

public:
	Utils();
	~Utils();
	float aleatorio();
	std::vector<float> generador(int n);
	
	//float distriucionNormal(float aleatorio1, float max, float min);
	
};